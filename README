= Ubuntu build notes =
 * Tested on a beaglebone running Ubuntu 13.10 (Saucy)

Download ubuntu
http://elinux.org/BeagleBoardUbuntu

# Building packages required.
apt-get install build-essential libxml2-dev libcurl4-openssl-dev flex

# Runtime packages required.
apt-get install lua5.1 lua-filesystem lua-penlight lua-rex-posix lua-socket

# Stopping apache2 as kloned will use port 80
update-rc.d -f  apache2 remove

# Build it
make

# Distribution will be placed into
./build/sysroot

# To package you need the packaging stuff
apt-get install packaging-dev

# To build an installable package for the armhf Architecture
make bb-package

# The resultant .deb file will be in build/
# Install like this
sudo dpkg -i build/*.deb


= Fedora build notes =
 * Tested on a Raspberry PI running Pidora 18

# You will need these pre-requisites installed to build
yum install subversion libxml2-devel libcurl-devel openssl-devel
yum groupinstall "Development Tools" --skip-broken
