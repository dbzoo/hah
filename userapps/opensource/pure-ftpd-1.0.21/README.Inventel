To build for MIPS

Don't rerun configure otherwise it will mess up the Makefiles

You can rebuild with:

make clean
AM_FLAGS="-static" make

Don't forget to strip the binaries

mips-uclibc-strip src/*
